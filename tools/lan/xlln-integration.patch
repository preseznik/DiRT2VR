diff --git a/xlivelessness/xlive/xlive.cpp b/xlivelessness/xlive/xlive.cpp
index 5c806a3..71167d4 100644
--- a/xlivelessness/xlive/xlive.cpp
+++ b/xlivelessness/xlive/xlive.cpp
@@ -309,6 +309,9 @@ HRESULT WINAPI XLiveInput(XLIVE_INPUT_INFO* xlive_input_info)
 // #5003
 void WINAPI XLiveUninitialize()
 {
+	DiRT2VRLanLog(4, 0, __func__, "Game requested network teardown.");
+	extern void DiRT2VRLanBrowserStop();
+	DiRT2VRLanBrowserStop();
 	TRACE_FX();
 	
 	XllnModulesPreXLiveUninitialize();
@@ -358,6 +361,7 @@ void WINAPI XLiveUninitialize()
 	xlln_remote_user_usernames.clear();
 	
 	LeaveCriticalSection(&xlive_critsec_remote_user);
+	DiRT2VRLanLogStop();
 }
 
 // #5010: This function is deprecated.
@@ -1616,6 +1620,8 @@ HRESULT WINAPI XLiveSignin(wchar_t* live_id_name, wchar_t* live_id_password, uin
 // #5297
 HRESULT WINAPI XLiveInitializeEx(XLIVE_INITIALIZE_INFO* xlive_initialise_info, uint32_t title_xlive_version)
 {
+	DiRT2VRLanLogStart();
+	DiRT2VRLanLog(4, 0, __func__, "instance=%08x transport_port=%hu title_version=%08x", xlln_global_instance_id, xlln_network_instance_port, title_xlive_version);
 	TRACE_FX();
 	
 	// if (IsDebuggerPresent())
@@ -1722,6 +1728,8 @@ HRESULT WINAPI XLiveInitializeEx(XLIVE_INITIALIZE_INFO* xlive_initialise_info, u
 	}
 	
 	xlive_initialised = true;
+	extern void DiRT2VRLanBrowserSeedPeer();
+	DiRT2VRLanBrowserSeedPeer();
 	return S_OK;
 }
 
diff --git a/xlivelessness/xlive/xsocket.cpp b/xlivelessness/xlive/xsocket.cpp
index dda6a11..e5c6150 100644
--- a/xlivelessness/xlive/xsocket.cpp
+++ b/xlivelessness/xlive/xsocket.cpp
@@ -15,6 +15,7 @@
 #include "../utils/utils.hpp"
 #include "../utils/util-socket.hpp"
 #include <map>
+#include <intrin.h>
 
 uint16_t xlive_port_online = 3074;
 uint16_t xlive_port_system_link = 0;
@@ -277,6 +278,8 @@ static void PassthroughStopReader_(XLIVE_TITLE_SOCKET* title_socket)
 // #4
 int32_t WINAPI XSocketClose(SOCKET title_socket_handle)
 {
+	DiRT2VRLanLogClose(title_socket_handle, _ReturnAddress());
+	XLLN_DEBUG_LOG(XLLN_LOG_LEVEL_INFO, "Game closing socket=%08x.", title_socket_handle);
 	TRACE_FX();
 	
 	const size_t packetSizeType = sizeof(XllnNetworkPacket::TYPE);
@@ -440,6 +443,7 @@ int32_t WINAPI XSocketClose(SOCKET title_socket_handle)
 // #5
 int32_t WINAPI XSocketShutdown(SOCKET title_socket_handle, int32_t how)
 {
+	XLLN_DEBUG_LOG(XLLN_LOG_LEVEL_INFO, "Game shutting down socket=%08x how=%d.", title_socket_handle, how);
 	TRACE_FX();
 	if (
 		how != SD_BOTH
@@ -2479,6 +2483,7 @@ int32_t WINAPI XSocketRecvFrom(SOCKET title_socket_handle, char* data_buffer, in
 	uint32_t received_size = 0;
 	int32_t resultXWsaRecvFrom = XWSARecvFrom(title_socket_handle, &wsabuf, 1, &received_size, &wsaFlags, address_from, address_from_size, 0, 0);
 	if (resultXWsaRecvFrom == ERROR_SUCCESS) {
+		DiRT2VRLanLogPacket(__func__, title_socket_handle, data_buffer, received_size, _ReturnAddress());
 		return received_size;
 	}
 	return SOCKET_ERROR;
@@ -2512,6 +2517,7 @@ int32_t WINAPI XSocketSendTo(SOCKET title_socket_handle, const char* data_buffer
 	uint32_t sent_size = 0;
 	int32_t resultXWsaRecvFrom = XWSASendTo(title_socket_handle, &wsabuf, 1, &sent_size, flags, address_to, address_to_size, 0, 0);
 	if (resultXWsaRecvFrom == ERROR_SUCCESS) {
+		DiRT2VRLanLogPacket(__func__, title_socket_handle, data_buffer, sent_size, _ReturnAddress());
 		return sent_size;
 	}
 	return SOCKET_ERROR;
diff --git a/xlivelessness/xlln/debug-log.hpp b/xlivelessness/xlln/debug-log.hpp
index 9754a81..d68f18f 100644
--- a/xlivelessness/xlln/debug-log.hpp
+++ b/xlivelessness/xlln/debug-log.hpp
@@ -46,8 +46,14 @@ void XllnTraceFunc(const char* function_name);
 #define XLLN_DEBUG_LOG(log_level, message_format, ...) XLLNDebugLogF(log_level, message_format, __VA_ARGS__)
 #define XLLN_DEBUG_LOG_ECODE(error_code, log_level, message_format, ...) XllnDebugLogECodeF(log_level, error_code, message_format, __VA_ARGS__)
 #define GET_SOCKADDR_INFO(sockAddrStorage) GetSockAddrInfo(sockAddrStorage)
-#define TRACE_FX() XllnTraceFunc(__func__)
-#else
+#define TRACE_FX() XllnTraceFunc(__func__)
+#elif defined(DIRT2VR_LAN_DIAGNOSTICS)
+#include "diagnostics.h"
+#define XLLN_DEBUG_LOG(log_level, message_format, ...) do { if (DiRT2VRLanLogEnabled()) DiRT2VRLanLog(log_level, 0, __func__, message_format, __VA_ARGS__); } while (0)
+#define XLLN_DEBUG_LOG_ECODE(error_code, log_level, message_format, ...) do { if (DiRT2VRLanLogEnabled()) DiRT2VRLanLog(log_level, error_code, __func__, message_format, __VA_ARGS__); } while (0)
+#define GET_SOCKADDR_INFO(sockAddrStorage) (DiRT2VRLanLogEnabled() ? GetSockAddrInfo(sockAddrStorage) : nullptr)
+#define TRACE_FX()
+#else
 #define XLLN_DEBUG_LOG(log_level, message_format, ...)
 #define XLLN_DEBUG_LOG_ECODE(error_code, log_level, message_format, ...)
 #define GET_SOCKADDR_INFO(sockAddrStorage) (0)
diff --git a/xlivelessness/xlln/xlln-modules.cpp b/xlivelessness/xlln/xlln-modules.cpp
index ff68ad3..8c0a533 100644
--- a/xlivelessness/xlln/xlln-modules.cpp
+++ b/xlivelessness/xlln/xlln-modules.cpp
@@ -174,6 +174,10 @@ static uint32_t InjectModuleEntryPointHook(HMODULE hModule)
 
 static void LoadXllnModules()
 {
+    extern void DiRT2VRLanInitialize();
+    DiRT2VRLanInitialize();
+    return; // LAN lab deliberately loads no additional third-party modules.
+
 	XLLN_DEBUG_LOG(XLLN_LOG_CONTEXT_XLLN_MODULE | XLLN_LOG_LEVEL_TRACE | XLLN_LOG_LEVEL_DEBUG
 		, "Hooked PE entry point invoked %s()."
 		, __func__
diff --git a/xlivelessness/xlln/xlln-network.cpp b/xlivelessness/xlln/xlln-network.cpp
index d4e8114..08e3d37 100644
--- a/xlivelessness/xlln/xlln-network.cpp
+++ b/xlivelessness/xlln/xlln-network.cpp
@@ -88,7 +88,11 @@ bool XllnNetEntityGetXnaddrXnkidByInstanceId(const uint32_t instanceId, XNADDR*
 		memcpy(&(xnaddr->abOnline), &instanceId, 4);
 		
 		memcpy((uint8_t*)&(xnaddr->abEnet) + 3, (uint8_t*)&mac_fix + 1, 3);
-		memcpy((uint8_t*)&(xnaddr->abOnline) + 17, (uint8_t*)&mac_fix + 1, 3);
+		memcpy((uint8_t*)&(xnaddr->abOnline) + 17, (uint8_t*)&mac_fix + 1, 3);
+		// XNetXnAddrToMachineId reads these eight bytes. Leaving them zero makes
+		// every generated peer address resolve to the same null machine identity.
+		const uint64_t machineId = instanceId;
+		memcpy(&xnaddr->abOnline[8], &machineId, sizeof(machineId));
 	}
 	
 	if (xnkid) {
@@ -732,8 +736,17 @@ static bool ParseNetworkData(uint8_t* data_buffer, size_t data_size, SOCKADDR_ST
 									
 									return true;
 								}
-								case XllnNetworkPacket::TCP_RESPONSE_CODE::XLLN_NP_TRC_KEEP_ALIVE: {
-									XLLN_DEBUG_LOG(XLLN_LOG_CONTEXT_XLIVE | XLLN_LOG_LEVEL_DEBUG
+								case XllnNetworkPacket::TCP_RESPONSE_CODE::XLLN_NP_TRC_KEEP_ALIVE: {
+									// Receiving a probe resets our idle timer above. Without a reply,
+									// a peer probing first can keep us silent until it times out.
+									// Use the existing ACK path, not another KEEP_ALIVE (which would echo).
+									if (titleSocket->tcpIsConnected) {
+										if (IsTimeUnset(titleSocket->tcpRecvLastAck)) {
+											TimeAddMillis(titleSocket->tcpRecvLastAck, 1000);
+										}
+										SetEvent(xlln_tcp_sockets_update);
+									}
+									XLLN_DEBUG_LOG(XLLN_LOG_CONTEXT_XLIVE | XLLN_LOG_LEVEL_DEBUG
 										, "%s %s packet %s from instanceId (0x%08x) titlePort (%hu)."
 										, __func__
 										, packetName
diff --git a/xlivelessness/xlln/xlln.cpp b/xlivelessness/xlln/xlln.cpp
index d16db16..664319c 100644
--- a/xlivelessness/xlln/xlln.cpp
+++ b/xlivelessness/xlln/xlln.cpp
@@ -1370,6 +1370,10 @@ bool InitXLLN(HMODULE hModule)
 {
 	bool xlln_debug_pause = false;
 	
+	wchar_t lanConfig[32768]{};
+	DWORD lanConfigSize = GetEnvironmentVariableW(L"DIRT2VR_LAN_CONFIG", lanConfig, 32768);
+	if (!lanConfigSize || lanConfigSize >= 32768) return false;
+	xlln_file_config_path = CloneString(lanConfig);
 	int nArgs;
 	// GetCommandLineW() does not need de-allocating but ToArgv does.
 	LPWSTR* lpwszArglist = CommandLineToArgvW(GetCommandLineW(), &nArgs);
